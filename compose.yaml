services:
  radial:
    build: .
    environment:
      RADAR_BIND: 0.0.0.0:8100
      RUST_LOG: radar_server=info
    ports: ["8100:8100"]
    volumes: ["radial-data:/data"]
    restart: unless-stopped
volumes:
  radial-data:

